#!/bin/sh

V=1.0.2g
F=$(download https://www.openssl.org/source/openssl-${V}.tar.gz)
unpack $F openssl-$V

try pushd .build/openssl-${V}
ARCH=${ARCH} CC=${CROSS_COMPILE}gcc try ./Configure --prefix=/usr linux-generic32 
try make -j${JOBS}
try make install INSTALL_PREFIX=${INSTALL_DIR}
popd
