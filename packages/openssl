#!/bin/sh

V=1.0.2d
F=$(download https://www.openssl.org/source/openssl-${V}.tar.gz)
unpack $F $V

pushd .build/openssl-${V}
ARCH=${ARCH} ./Configure --prefix=/usr linux-generic32
make -j${JOBS}
make install INSTALL_PREFIX=${INSTALL_DIR}
popd
